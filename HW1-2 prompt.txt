請撰寫一段完整且可執行的 Python 程式碼來完成以下任務，並確保沒有語法錯誤：

1. 讀取 CSV 資料：從 "C:\Users\jing5\Documents\HW1-2\2330-training.csv" 中讀取資料。
2. CSV 資料格式：
   - Date：日期欄位，格式 %m/%d/%Y。
   - y：股票價格欄位（含逗號的數字格式）。
3. 資料處理：
   - 將 Date 欄位轉換為日期格式（datetime）。
   - 移除 y 欄位中的逗號（','），並將其轉換為 float。
4. 使用 Prophet 模型預測：
   - 預測 y（股票價格）。
   - 設定變化點靈敏度為 0.5。
   - 設定 95% 的不確定性區間。
   - 增加每月季節性（Fourier Order 設為 5）。
5. 預測未來 60 天的股票價格，並使用 matplotlib 畫出預測結果圖。
6. 圖表需求：
   - 顯示實際數據（黑色線條）。
   - 預測數據（藍色線條），以及不確定性區間（淺藍色陰影）。
   - 添加一條藍色實線表示「Historical Average」。
   - 在預測初始化點加上紅色虛線標註「Forecast Initialization」。
7. 確保程式碼遵循 Python 語法，且可以正常執行。請注意括號的配對和正確的縮排。
8. 確保代碼適用於 Python 3.6 或以上版本。